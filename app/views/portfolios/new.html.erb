<h1>Create a new Portfolio Item</h1>

<%= form_for(@portfolio_items) do |f| %>
  <% if @portfolio_items.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(portfolio_items.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% portfolio_items.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>